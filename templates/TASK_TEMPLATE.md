---
id: TAG-NNN
project: <Server | STC | SRC | DAI | Sensor HUB>
workspace: {{ROOT}}{{SEP}}<P>
status: pending          # pending | in_progress | reported | verified | rejected | done
created: YYYY-MM-DD
architect: Claude
hil_required: false      # true лише для задач, що потребують фізичного тесту (Sensor HUB, прошивки)
model: flash-3.8 high   # рекомендація Архітектора: flash-3.8 high (дефолт) | pro-3.1 | claude-opus — fallback після двох REJECTED поспіль або зациклення
subagents: review       # review (дефолт) | research | none — read-only субагенти, кількість не обмежена; review = незалежна перевірка перед Report (збирає ДОКАЗИ, вердиктів не виносить); research = дослідження + перевірка; none — лише за вказівкою Gans і означає ЗАБОРОНУ субагентів у цій задачі
related: []              # ID задач в інших проєктах для крос-проєктних фіч
---

# FATAL BOUNDARY CHECK
Перед будь-якою дією виконай `pwd`. Очікуваний воркспейс: `{{ROOT}}{{SEP}}<P>`.
Якщо шлях не збігається — НЕГАЙНО зупинись, виведи `FATAL ERROR: wrong workspace` і відмовся від задачі.
Ти не маєш права змінювати нічого за межами цього воркспейсу.

# [SSOT Context]
- `Context_<X>.md` → розділ «…»: <цитата або переказ 1–3 речення>
- `Plan_<X>.md` → пункт «…»

# Мета
<Одне речення: що має стати правдою після виконання.>

# Обсяг і кроки
1. …
2. …

Проведи самостійний глибокий аудит дотичного коду перед реалізацією. Спосіб реалізації — на твій розсуд у межах Обмежень.
Якщо в ТЗ є логічна дірка або відсутня залежність — не виконуй наосліп: виправ, якщо очевидно, або зупинись і опиши в Report.

# Обмеження
- …
- Видалення даних понад поодинокі файли — лише після явної згоди через `ask_question`, навіть якщо крок описано в ТЗ. «Злити», «дедуплікувати», «почистити» не означає «видалити».
- Жодних деплоїв на віддалені цілі, прошивок/стирань пристроїв та інших ADLP-дій без явної команди Gans.

# Definition of Done
- [ ] <збірка/тести зелені: команда>
- [ ] `Context_<X>.md` оновлено (якщо зачеплено архітектуру)
- [ ] Запис у `Changelog_<X>.md`; виконаний пункт ВИДАЛЕНО з `Plan_<X>.md`
- [ ] `git status` чистий (scratch-файли прибрано), `git commit` + `git push`; хеш у Report
- [ ] Report нижче заповнено, `status: reported`

<!-- ============ нижче заповнює L1 Developer ============ -->
# Report
## Що зроблено

## Верифікація (команди + короткий вивід)

## Мутаційна перевірка
<Для кожної мутації: файл, рядок, було → стало; команда; вивід тесту, що впав. Обовʼязково при `hil_required: true`. Мутація, яка вижила, називається прямо: гілка не покрита тестами. Заміри — з чистої копії коміту.>

## Errors & Obstacles
<обов'язково, навіть якщо все пройшло гладко — тоді «Немає»>

## SSOT
- [ ] Context  - [ ] Plan  - [ ] Changelog  - [ ] USER_GUIDE (якщо змінилась поведінка для користувача)
- commit: `<hash>`
- Зміни, що зачіпають крос-проєктні контракти або інші підпроєкти: <так/ні, що саме>

<!-- ============ нижче заповнює L2 Architect ============ -->
# Verdict
