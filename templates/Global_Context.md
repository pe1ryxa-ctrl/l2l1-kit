# {{ECOSYSTEM}} — Global Context

## Екосистема
<Опис системи в цілому: призначення, компоненти, як вони взаємодіють. Веде Claude (L2).>

1.  **<Підпроєкт 1>**
    *   **Роль:** …
    *   **Технології:** …
    *   **Взаємодія:** …

## Ролі та AI-воркфлоу
| Хто | Роль |
|---|---|
| **Gans** | Керівник. Затверджує ТЗ, тригерить L1 (`/task <ID>`), тестує залізо, дає згоду на ADLP-дії та деплої. |
| **Claude** | **L2 Architect.** Планує, пише задачі у `<P>/.agents/tasks/<ID>.md`, верифікує по `git diff`, веде Global_* і `.agents/`. Протокол: `CLAUDE.md`. |
| **Gemini (Antigravity)** | **L1 Developer.** Один воркспейс = один підпроєкт. Реалізує, тестує, оновлює Holy Trinity, комітить, дописує Report. Контракт: `<P>/.agents/AGENTS.md`. |

- **SSOT:** Holy Trinity (`Context_X / Plan_X / Changelog_X`) веде L1; Global_* веде Claude. Plan містить лише беклог.
- **Задачі:** шаблон `.agents/TASK_TEMPLATE.md`; ID — `<TAG>-<NNN>`; файли комітяться в git підпроєкту; закриті — у `tasks/done/`.

## Крос-проєктні контракти
<Інтерфейси між підпроєктами: API, протоколи, формати повідомлень, порти. Кожна зміна тут — задачі в усі дотичні підпроєкти.>
